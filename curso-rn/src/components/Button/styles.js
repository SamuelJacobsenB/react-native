import { StyleSheet } from "react-native";

export const s = StyleSheet.create({
  button: {
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    width: 150,
    marginBottom: 10,
    padding: 10,
    borderRadius: 10,
  },
});
